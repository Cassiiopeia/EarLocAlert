# 이 파일을 .env 로 복사해서 값을 채운다. .env 는 커밋되지 않는다.
#
#   cp .env.example .env
#
# Google Maps API 키 (docs/08-OPERATIONS.md)
#
# Google Cloud 콘솔에서 아래 두 API 를 켜고 키를 발급한다.
#   - Maps SDK for Android
#   - Maps SDK for iOS
#
# 비워두면 지도만 회색으로 뜨고 나머지 기능은 정상 동작한다 —
# 키가 없는 사람도 빌드할 수 있어야 한다.
MAPS_API_KEY=
