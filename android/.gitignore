# gradle-wrapper.jar — CI 빌드에 필요하여 커밋한다 (내부망 개발환경에서 재생성 불가)
/.gradle
/captures/
# /gradlew — 위와 동일
# /gradlew.bat — 위와 동일
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Android signing

app/keystore/*.jks

app/keystore/*.keystore
