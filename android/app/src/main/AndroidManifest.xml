<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ==========================================================
         권한 (docs/05-PLATFORM.md)

         백그라운드 위치는 Play 심사에서 별도 권한 선언 양식과
         기능 시연 영상을 요구한다. 반려율이 높은 항목이므로
         docs/09-RELEASE.md 의 사유서를 함께 준비한다.
         ========================================================== -->

    <!-- 위치 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- API 29+ — 앱을 열지 않아도 도착·출발을 알리기 위해 필요하다.
         API 30+ 는 앱 내 다이얼로그로 받을 수 없어 설정 화면으로 보낸다. -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- 포그라운드 서비스 — Android 는 이것으로 정밀 감시한다.
         사용자에게 상시 알림이 보이며, 이는 없앨 수 없다. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- API 34+ 필수 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />

    <!-- 알림 — API 33+ 런타임 요청 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 진동 (F3.1) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- 재부팅 후 감시 복구 (A-10).
         지오펜스 상태는 DB 에 있으므로 서비스만 다시 띄우면 된다. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- 전체화면 알림 — Android 14+ 는 알람·통화 계열에만 자동 부여되므로
         설정 화면에서 사용자가 직접 켜야 한다. 화면이 꺼졌거나 잠긴 상태를
         담당한다. 거부돼도 앱은 고중요도 알림 + 반복 진동으로 성립한다
         (docs/10-DECISIONS.md 006 재검토, 이슈 #74). -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- 다른 앱 위에 표시 (이슈 #74) — 화면이 켜져 있을 때 알림 화면으로
         화면을 덮는 유일한 수단이다. 전체화면 알림은 잠금화면에서만
         액티비티를 띄우고, 사용 중에는 헤드업 알림으로 강등된다.
         백그라운드 액티비티 시작 제한의 면제 조건이기도 하다.
         설정 화면에서만 켤 수 있고, 없어도 앱은 동작한다. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 배터리 최적화 예외 (이슈 #74) — Doze 와 제조사 절전이 지오펜스
         콜백을 묶어서 늦게 전달하면 "버스에서 내릴 곳" 알림은 실패다.
         Play 심사에 사유 설명이 필요하다 (docs/09-RELEASE.md). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- 광고 식별자 — Play 데이터 보안 양식에 수집 신고가 필요하다
         (docs/09-RELEASE.md) -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <!-- 네트워크 — 광고와 앱 업데이트 확인에 쓴다. 위치는 전송하지 않는다 -->
    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:label="이어폰위치알림"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- showWhenLocked·turnScreenOn (이슈 #74) — 전체화면 알림과 감시
             서비스가 이 액티비티를 띄운다. 잠긴 화면 뒤에서 조용히 실행만
             되면 알림이 아니다. API 27 미만은 속성을 무시하며 그 경우
             알림과 진동으로 성립한다. MainActivity 가 코드로도 같이 건다. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- AdMob 앱 ID (docs/07-MONETIZATION.md).
             **공개 정보라 소스에 둔다** — APK 를 뜯으면 그대로 나오므로
             숨기는 것이 의미가 없다. Maps 키와 달리 과금 대상도 아니다.

             **`.env` 로 빼지 않는 이유** — google_mobile_ads 는 이 값이
             없으면 앱 시작 시 크래시한다. 키가 없는 사람도 빌드할 수
             있어야 하므로 값이 항상 있어야 한다.

             실제 광고가 나가는지는 이 값이 아니라 **광고 단위 ID** 가
             정한다 (AdUnitIds) — 디버그 빌드는 언제나 테스트 단위를 쓴다. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4452677329657064~8983198402"/>

        <!-- Google Maps API 키 (docs/08-OPERATIONS.md).
             값은 build.gradle.kts 가 레포 루트 .env 에서 읽어 치환한다.
             AdMob 앱 ID 와 달리 이 키는 과금 대상이라 소스에 두지 않는다.
             비어 있으면 지도만 회색으로 뜨고 앱은 정상 동작한다. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />


        <!-- native_geofence (docs/10-DECISIONS.md 014) —
             OS 지오펜스 이벤트 수신, 재부팅 후 자동 재등록(A-10),
             콜백용 서비스. 재등록 리시버 덕에 앱이 부팅 브로드캐스트를
             직접 처리할 필요가 없다. -->
        <receiver android:name="com.chunkytofustudios.native_geofence.receivers.NativeGeofenceBroadcastReceiver"
            android:exported="true"/>
        <receiver android:name="com.chunkytofustudios.native_geofence.receivers.NativeGeofenceRebootBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
        <service android:name="com.chunkytofustudios.native_geofence.NativeGeofenceForegroundService"
            android:permission="android.permission.BIND_JOB_SERVICE" android:exported="true"/>

        <!-- 감시 상시 유지·알림 발화 서비스 (이슈 #74, #93).
             native_geofence 의 서비스는 콜백 실행용 단발성이라 프로세스를
             살려두지 못한다. 이 서비스가 절전 중에도 감시를 유지하고,
             Flutter 엔진을 상시 보유해 지오펜스 이벤트를 직접 판정하며,
             알림이 발생하면 반복 진동과 함께 앱을 전면으로 띄운다.
             위치 감시가 존재 이유이므로 location 타입이다. -->
        <service
            android:name=".AlertWatchService"
            android:exported="false"
            android:foregroundServiceType="location" />

        <!-- 지오펜스 이벤트 수신 (이슈 #93).
             native_geofence 의 리시버는 이벤트를 WorkManager 로 넘겨
             즉시 실행 쿼터 소진 시 강등되고, 작업 체인이 한 번 실패하면
             이후 이벤트가 전부 사라진다. Android 는 앱이 직접 등록하고
             이 리시버가 받아 감시 서비스로 곧바로 넘긴다. -->
        <receiver
            android:name=".GeofenceReceiver"
            android:exported="false" />

        <!-- 재부팅 후 감시 복구 (이슈 #93).
             위의 RECEIVE_BOOT_COMPLETED 권한과 주석은 있었으나 정작 이
             리시버가 없어, 재부팅 후 앱을 켜지 않으면 감시가 죽은 채로
             남아 있었다. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
