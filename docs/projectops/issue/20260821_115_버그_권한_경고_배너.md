🗒️ 설명
---

**알림 신뢰성 권한이 꺼져 있어도 홈 화면에 경고가 뜨지 않는다.** 그리고 그 원인이 무한 재계산이었다.

`homeStatusProvider` 가 `permissionControllerProvider` 를 `watch` 하는데, 둘 다 자동 폐기(autoDispose) 대상이라 다음 고리가 생긴다.

```
homeStatus 빌드 → permissionController watch (생성 · 로딩 시작)
     ↑                              ↓
     └──── 무효화 ←──── 로딩 완료로 상태 변경
```

`homeStatus` 가 다시 계산되는 순간 구독이 잠깐 끊기고, 자동 폐기 대상이라 즉시 버려진 뒤 처음부터 다시 로딩된다. **결과적으로 권한 조회가 영원히 "진행중"으로 남는다.**

```dart
final reliable = ref.watch(permissionControllerProvider)
    .valueOrNull?.canAlertReliably ?? true;   // ← 항상 true 로 떨어진다
```

진단 기록으로 확인한 실제 동작:

```
[home] 상태 감시=true 이어폰=false 알림신뢰=true (권한조회=진행중)
[home] 상태 감시=true 이어폰=false 알림신뢰=true (권한조회=진행중)   ← 0.03초 간격
[home] 상태 감시=true 이어폰=false 알림신뢰=true (권한조회=진행중)
```

**초당 30회가량 권한 조회 채널이 불린다.** 배너가 안 뜨는 것뿐 아니라 배터리도 갉아먹는다.

**배너 자체도 약했다.** 회색 알약에 한 줄이라 지도 위에서 묻히고, 문구가 잘려 무엇이 문제인지 읽히지 않았다.

```
절전·다른 앱 사용 중에는 알림이 약합니다 · 눌러서 ...
```

🔄 재현 방법
---

1. 배터리 최적화 제외를 끈 상태로 앱을 연다
2. 홈 화면에 아무 경고도 뜨지 않는다
3. 설정 → 알림 도달 권한에서는 "미허용"으로 정확히 표시된다

📸 참고 자료
---

- `lib/app/home_status_provider.dart` — `permissionControllerProvider` 를 watch
- `lib/features/permission/presentation/permission_controller.dart` — `@riverpod` (자동 폐기)
- `lib/features/places/presentation/place_map_home_screen.dart` — `_WeakAlertBanner`

✅ 예상 동작
---

- 신뢰성 권한이 하나라도 꺼져 있으면 **홈에 눈에 띄는 경고가 뜬다**
- **무엇이 꺼졌는지 이름으로 알려준다** — "알림이 약합니다"만으로는 무엇을 해야 할지 알 수 없다
- 지도 위에서도 읽힌다 (반투명 금지)
- 권한 조회는 필요할 때만 — 앱 시작과 시스템 설정에서 돌아왔을 때

⚙️ 환경 정보
---

- **OS**: Android
- **버전**: v1.10.0
